---
title: Application API
description: Mount Flue routes and configure providers through the application runtime API.
---

## Imports from `@flue/runtime/app`

TODO: Document the application composition entry point for authored `app.ts` modules.

## `flue()`

TODO: Reference the public application routes for agents, workflows, runs, and OpenAPI output.

## `admin()`

TODO: Reference read-only administrative inspection routes.

## `Fetchable`

TODO: Document the mountable application contract.

## Provider configuration

TODO: Reference `configureProvider(...)`, `registerProvider(...)`, `registerApiProvider(...)`, and associated provider types.

## Dispatch and observation

TODO: Cross-reference `dispatch(...)` and `observe(...)` where exposed for application logic.
