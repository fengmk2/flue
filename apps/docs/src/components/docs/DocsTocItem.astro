---
interface Props {
	depth: number;
	slug: string;
	text: string;
}

const { depth, slug, text } = Astro.props;
---

<li class:list={['[&+&]:mt-1', { 'pl-3': depth === 3 }]}>
	<a class="text-sm leading-tight text-gray-500 hover:text-gray-950" href={`#${slug}`}>{text}</a>
</li>
