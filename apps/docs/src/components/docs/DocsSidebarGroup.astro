---
import type { DocsNavGroup } from '../../lib/docs-navigation';
import DocsSidebarLink from './DocsSidebarLink.astro';

interface Props {
	group: DocsNavGroup;
	currentSlug: string;
	mobile?: boolean;
}

const { group, currentSlug, mobile = false } = Astro.props;
---

<section class:list={[mobile ? 'pb-3 [&+&]:pt-3' : 'pb-6 [&+&]:pt-1.5']}>
	<h2 class="mb-2 text-sm font-semibold tracking-normal text-gray-700">{group.title}</h2>
	<ul class="m-0 list-none p-0">
		{group.items.map((item) => (
			<li><DocsSidebarLink item={item} currentSlug={currentSlug} /></li>
		))}
	</ul>
</section>
